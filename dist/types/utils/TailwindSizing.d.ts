import type { TailwindSize } from '@/types';
export declare function resolveTailwindSize(size: TailwindSize, parentSize: number): number;
export declare const FRACTION_PRESETS: Record<string, number>;
